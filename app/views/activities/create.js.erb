// Insert new activity onto the page
$("<%= escape_javascript render('tr', activity: @activity) %>").prependTo("#activities table");

$('#<%= add_delete_lead_button_id(@lead.response_id) %>').fadeOut(function() { $(this).remove(); });

// Update the lead status, it may have changed
$("form.edit-lead input[type='radio'][value='<%= @lead.status_id %>']").prop('checked', true);
