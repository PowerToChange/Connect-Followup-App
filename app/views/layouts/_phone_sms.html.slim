- if tel.present?
  phone tel= number_to_phone(tel) tourl= survey_responses_path(survey_id: survey_id, id: response_id)
  sms tel= number_to_phone(tel) tourl= survey_responses_path(survey_id: survey_id, id: response_id)