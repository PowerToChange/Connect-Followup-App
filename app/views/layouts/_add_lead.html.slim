- if !Lead.all.map(&:response_id).include?(response.response.id)
  = link_to_icon 'plus-sign', survey_leads_path(survey, lead: { response_id: response.response_id, contact_id: response.contact_id }), method: :post, label_hidden_phone: 'Add', class: 'btn', id: create_lead_button_id(response.response.id), data: { remote: true }
- else
  = link_to_icon 'ok-sign', 'javascript:void(0)', class: 'btn disabled'